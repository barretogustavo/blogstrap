<div class="card mb-3">
  <div class="card-body">
    <h5 class="card-title">
      <%= comment.user.email %>
    </h5>
    <h6 class="card-subtitle mb-2 text-muted">
      <%= date_formatter(comment.created_at)%>
    </h6>
    <p class="card-text"><%= comment.body %></p>
  </div>
</div>
